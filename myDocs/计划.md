# 工作计划

2026-07-05前

1. 新增方案择优选择算法（采用优劣解距离法TOPSIS）--done
2. 解决多目标值域、量纲、波动幅度差异巨大时，隐形拉高单选权重，导致主观权重配置失效的问题。--done

2026-07-13前

1. 解决优化目标数超过4个时，拥挤度机制完全失效，使得解的分布极度不均匀，大量解聚集在局部区域，帕累托前沿覆盖不全，导致帕累托支配关系失效问题；同时，精英策略会保留上一代所有非支配解，低维下非支配解数量少，能加速收敛；高维下非支配解数量爆炸，大量平庸解占据种群空间，新生的优质解难以存活进入下一代。导致迭代几十代后种群就不再进化，指标陷入停滞，陷入局部最优无法跳出。------用参考点关联机制替代拥挤距离 --done
2. 解决参数配置，




# 后续优化点

优化工序排程时找不到设备，找不到工人时的处理逻辑---done
优化排程算法为nsga3型
优化工作日有多个休息时段的----done
优化订单给的是交付日期情形---done

优化工作日历模型---done
优化人员在某天请假，某天不请假情形 worker_aviable_dict={WorkId:{Datetime:bool}},
get_machine_overload_penalty,这个函数中过载应该是需要算出排程总的工作日数，应该判定：总的时间 > 排程工作日数 * 每天计划小时
步骤1：筛选所有可优化浮动工序，同订单内按工艺编号升序 ======================这里优化代码的重复
优化这个函数generate_heuristic_chromosome中，应该按照交付日期进行排序，越早的优先排程
